os: osx
osx_image: xcode10.3
language: swift

install:
  - ./install_swiftlint.sh

script:
  - xcodebuild build test -workspace Calculator/Calculator.xcworkspace -scheme "Calculator" -sdk iphonesimulator -destination "platform=iOS Simulator,OS=12.1,name=iPhone 8" ONLY_ACTIVE_ARCH=NO CODE_SIGNING_REQUIRED=NO
